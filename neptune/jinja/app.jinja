apiVersion: management.astra.netapp.io/v1alpha1
kind: Application
metadata:
  name: {{ appName }}
  namespace: neptune-system
spec:
  includedNamespaces:
    - namespace: {{ namespace }}
{%- if labelSelectors %}
      labelSelector: {{ labelSelectors }}{% endif %} 
{%- if addNamespaces %}
{{ addNamespaces }}
{%- endif %}
{%- if clusterScopedResources %}
  includedClusterScopedResources:
{{ clusterScopedResources }}
{%- endif %}
